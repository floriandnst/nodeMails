# nodeMails - node module for sending mails via C#

## Introduction

This node module can be used for sending emails with node.js and c#.<br/>
It can be download with npm (node package manager)

```shell
npm i nodeMails
```