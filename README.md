# nodeMails - node module for sending mails via C# application

## Introduction

This node module can be used for sending emails with node.js and c#.
It can be download with npm (node package manager)

```shell
npm i nodeMails
```